[tunnel]
tunnel_name = smtp_tunnel

# Cloud SSH server endpoint
server_host = your-cloud-server.example.com
server_port = 22

# Port exposed on the cloud server that remote clients will connect to
# Example: clients reach SMTP on your-cloud-server.example.com:10025
port = 10025

# Keep-alive interval in seconds
keep_alive_time = 30

# Local SMTP service to forward to
remote_host = 127.0.0.1
remote_port = 25            # SMTP: 25 | submission: 587 | SMTPS: 465

# SSH authentication — passwordless key required (no passphrase)
# Path is relative to this config file's directory
keyfile = keyfile_smtp
username = tunnel_user_smtp

# Cloud server's public host key for strict host verification
# Path is relative to this config file's directory
server_key = known_server_smtp

log_level = INFO
